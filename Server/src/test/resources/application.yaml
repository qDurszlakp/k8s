spring:
  liquibase:
    change-log: "classpath:liquibase-changeLog.xml"

  jpa:
    open-in-view: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

files:
  baseDir: "/app/data/Data.txt"

data:
  exampleUri: "https://jsonplaceholder.typicode.com/posts"
  passphraseUrl: "http://foo:8091/"

audit:
  patterns: '/**'